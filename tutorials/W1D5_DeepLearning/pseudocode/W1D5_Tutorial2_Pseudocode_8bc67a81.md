# Pseudocode twin — W1D5 Tutorial 2: Complete and uncomment

- **Answer twin:** `../solutions/W1D5_Tutorial2_Solution_8bc67a81.py`
- **Reading view:** `../W1D5_Tutorial2_pseudocode.md`

Fill each `____`. Comments above a blank are the hint; the completed answer is the sibling `.py`.

```text
# get weights of conv layer in convLayer
weights = ____
print(weights.shape)  # can you identify what each of the dimensions are?

with plt.xkcd():
  plot_weights(weights, channels=np.arange(0, out_channels))
```

Blanks:
1. `weights` — get weights of conv layer in convLayer
