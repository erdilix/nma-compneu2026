# Pseudocode twin — W2D3 Tutorial 4: Insert your code here take to compute the residual (error)

- **Answer twin:** `../solutions/W2D3_Tutorial4_Solution_4d89c578.py`
- **Reading view:** `../W2D3_Tutorial4_pseudocode.md`

Fill each `____`. Comments above a blank are the hint; the completed answer is the sibling `.py`.

```text
# compute the predicted values using the autoregressive model (lam_hat), and
# the residual is the difference between x2 and the prediction
res = ____

# Visualize
with plt.xkcd():
  plot_residual_histogram(res)
```

Blanks:
1. `res` — the residual is the difference between x2 and the prediction
